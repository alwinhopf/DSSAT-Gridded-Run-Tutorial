[pytest]
markers =
    integration: live API or external-service tests excluded from fast health checks
    slow: long-running tests excluded from fast health checks
testpaths = tests
norecursedirs = renv .git dssat_runs results soil weather venv .venv .Rproj.user
filterwarnings =
    ignore:numpy.ndarray size changed:RuntimeWarning

