# Deprecated: Python dependencies are managed by environment.yml + conda-lock.
#
# Do not install this project with:
#   pip install -r requirements.txt
#
# Use:
#   conda install -c conda-forge conda-lock
#   conda-lock -f environment.yml -p win-64 -p linux-64
#   conda-lock install -n dssat_grid conda_lock.yml
#
# For HPC systems where mpi4py must be built against the cluster MPI, create the
# conda-lock environment first, then install mpi4py after loading the MPI module:
#   pip install --no-cache-dir --no-binary :all: mpi4py==3.1.6
